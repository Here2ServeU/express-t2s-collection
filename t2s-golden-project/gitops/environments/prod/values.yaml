image:
  repository: "REPLACE_WITH_REGISTRY/express-t2s"
  tag: "latest"

replicaCount: 4

ingress:
  enabled: true
  hosts:
    - host: "prod.express.local"
      paths:
        - path: /
          pathType: Prefix
